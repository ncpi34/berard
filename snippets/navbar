 <ul class="navbar-nav mx-auto">
            {% for item in left_menu %}
                <li class="nav-item dropdown">

                <a href="#"
                   class="nav-link dropdown-toggle text-white"
                    data-toggle="dropdown">
                    <i class="{{ item.icon_class }}" ></i> {{ item.name }}
                </a>

                {% if item.submenu %}
                    <div class="dropdown-menu">
{#                    <ul class="collapse list-unstyled" id="{{ item.name }}">#}
                    {% if item.submenu.count > 5 %}
                    {% for menu in item.submenu %}
{#                        <li class="{% if menu.selected %} active {% endif %}">#}
{#                        <a href="{{ menu.get_absolute_url }}" id="submenu"#}
{#                           class="text-white text-center">{{ menu.name }}</a>#}
                            <a href="{% url 'website:products_by_familly' nom=menu.name %}"
                               class="dropdown-item"
                            >{{ menu.name }}</a>

                    {% endfor %}
                        {% else %}
                        {% for menu in item.submenu %}
{#                        <li class="{% if menu.selected %} active {% endif %}">#}
{#                        <a href="{{ menu.get_absolute_url }}" id="submenu"#}
{#                           class="text-white text-center">{{ menu.name }}</a>#}
                            <a href="{% url 'website:products_by_familly' nom=menu.name %}"
                               id="submenu"
                               class="dropdown-item"
                            >{{ menu.name }}</a>

                    {% endfor %}
                        {% endif %}
                    </div>
                        </li>
             {% endif %}
            {% endfor %}
            </ul>